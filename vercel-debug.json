{
  "version": 2,
  "buildCommand": "echo '🔧 Initial working directory:' && pwd && echo '🔧 Listing current directory:' && ls -la && echo '🔧 Looking for apps directory:' && find . -name 'apps' -type d 2>/dev/null || echo 'apps directory not found' && echo '🔧 Looking for web directory:' && find . -name 'web' -type d 2>/dev/null || echo 'web directory not found' && echo '🔧 Attempting to navigate to apps/web:' && cd apps/web && echo '🔧 Successfully in apps/web:' && pwd && NEXT_PUBLIC_USE_STANDALONE_OUTPUT=false pnpm build && echo '🔧 Build output verification:' && ls -la .next/",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "env": {
    "NEXT_PUBLIC_USE_STANDALONE_OUTPUT": "false"
  },
  "functions": {
    "apps/web/src/pages/api/**/*.ts": {
      "runtime": "nodejs18.x",
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
