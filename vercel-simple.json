{
  "version": 2,
  "buildCommand": "pwd && cd apps/web && pwd && NEXT_PUBLIC_USE_STANDALONE_OUTPUT=false pnpm build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "env": {
    "NEXT_PUBLIC_USE_STANDALONE_OUTPUT": "false"
  },
  "functions": {
    "apps/web/src/pages/api/**/*.ts": {
      "runtime": "nodejs18.x",
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
