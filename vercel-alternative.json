{
  "version": 2,
  "buildCommand": "ls -la && pwd && pnpm install --frozen-lockfile && ls -la apps/ && cd ./apps/web && pnpm build",
  "outputDirectory": "apps/web/.next",
  "installCommand": null,
  "framework": null,
  "functions": {
    "apps/web/src/pages/api/**/*.ts": {
      "runtime": "nodejs18.x",
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
